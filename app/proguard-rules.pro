# The game uses no reflection, so the defaults are enough.
# Keep line numbers to make release crash reports readable.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
